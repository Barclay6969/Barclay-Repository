name: Build MoFlix Movie2k test ZIP

on:
  push:
    branches: [moflix-test-20260914]
    paths:
      - '.github/workflows/build-moflix-test.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: moflix-test-20260914
      - run: python3 tools/moflix-test/build_test.py "$GITHUB_WORKSPACE"
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add zips/test/plugin.video.xship-2026.09.14.91-MOFLIX-MOVIE2KTEST.zip
          git commit -m 'Build xShip 2026.09.14.91 MoFlix Movie2k test ZIP'
          git push origin HEAD:moflix-test-20260914
