--- xShip/streamcloudforum.py
+++ xVault/streamcloudforum.py
@@ -14,8 +14,6 @@
     from HTMLParser import HTMLParser
     html_unescape = HTMLParser().unescape
 
-from resources.lib.domain_manager import resolve_domain
-
 SITE_IDENTIFIER = 'streamcloudforum'
 SITE_DOMAIN = 'streamcloud.forum'
 SITE_NAME = 'STREAMCLOUD.FORUM'
@@ -25,7 +23,7 @@
     def __init__(self):
         self.priority = 2
         self.language = ['de']
-        self.domain = resolve_domain(SITE_IDENTIFIER, SITE_DOMAIN)
+        self.domain = getSetting('provider.' + SITE_IDENTIFIER + '.domain', SITE_DOMAIN)
         self.base_link = 'https://' + self.domain
         self.search_link = self.base_link + '/index.php?do=search&subaction=search&story=%s'
         self.sources = []
